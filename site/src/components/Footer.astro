---
const links = [
  { label: "New Machine Setup", url: "/new-machine" },
  { label: "GitHub", url: "https://github.com/jordanburke/agent-env" },
  { label: "Issues", url: "https://github.com/jordanburke/agent-env/issues" },
  { label: "MIT License", url: "https://github.com/jordanburke/agent-env/blob/main/LICENSE" },
]

const related = [
  { label: "SOPS", url: "https://github.com/getsops/sops" },
  { label: "age", url: "https://github.com/FiloSottile/age" },
  { label: "direnv", url: "https://direnv.net/" },
  { label: "dotenvx", url: "https://dotenvx.com/" },
]
---

<footer class="border-t border-border-subtle px-6 py-12">
  <div class="mx-auto max-w-4xl">
    <div class="flex flex-col items-center gap-8 md:flex-row md:justify-between">
      <div>
        <div class="mb-2 text-sm font-bold text-terminal-green">agent-env</div>
        <p class="text-xs text-terminal-dim">Universal secret injection for AI coding agents.</p>
      </div>

      <div class="flex gap-6">
        {
          links.map((link) => (
            <a
              href={link.url}
              target="_blank"
              rel="noopener noreferrer"
              class="text-xs text-terminal-dim transition-colors hover:text-terminal-green"
            >
              {link.label}
            </a>
          ))
        }
      </div>
    </div>

    <div class="mt-8 border-t border-border-subtle pt-6">
      <p class="mb-3 text-xs text-terminal-dim">Related projects:</p>
      <div class="flex flex-wrap gap-3">
        {
          related.map((r) => (
            <a
              href={r.url}
              target="_blank"
              rel="noopener noreferrer"
              class="rounded border border-border-subtle px-2 py-1 text-xs text-terminal-dim transition-colors hover:border-terminal-green/40 hover:text-terminal-green"
            >
              {r.label}
            </a>
          ))
        }
      </div>
    </div>

    <p class="mt-8 text-center text-xs text-terminal-dim">
      MIT &copy; {new Date().getFullYear()} Jordan Burke
    </p>
  </div>
</footer>
