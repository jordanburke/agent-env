# Minimal .sops.yaml for a single-project setup
# Place at repo root next to .sops.env
#
# Replace the age: value with your public key from:
#   grep "public key:" ~/.config/sops/age/keys.txt

creation_rules:
  - path_regex: \.sops\.env$
    age: >-
      age1your_public_key_here
